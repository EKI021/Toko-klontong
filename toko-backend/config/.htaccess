# Blokir akses langsung ke folder ini lewat browser (Apache 2.4+)
<IfModule mod_authz_core.c>
    Require all denied
</IfModule>
# Fallback untuk Apache versi lama (2.2)
<IfModule !mod_authz_core.c>
    Order deny,allow
    Deny from all
</IfModule>
